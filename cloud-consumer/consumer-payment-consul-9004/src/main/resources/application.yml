server:
  port: 9004

spring:
  application:
    name: consumer-payment-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        #hostname: 127.0.0.1
        service-name: ${spring.application.name}